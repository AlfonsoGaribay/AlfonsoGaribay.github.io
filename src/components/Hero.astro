---
export interface Props {
	title: string;
	subtitle: string;
}

const {title, subtitle } = Astro.props;
---


<section class="w-screen">
    <div class="h-screen bg-hero-wallpaper bg-cover bg-center">
        <div class="text-slate-100 pl-12 pr-12 hero-title">
            <h1 class="font-header font-bold text-GIGA">{title}</h1>
            <h3 class="font-nonheader font-semibold text-7xl">{subtitle}</h3>
        </div>  
    </div>
</section>

<style>
    .hero-title {
      opacity: 0;
      animation: fadeInAnimation 3s ease-in forwards;
    }
    
    @keyframes fadeInAnimation {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    </style>