--- 
export interface props {
    title: string
}

const { title } = Astro.props
---

<section class="max-w-7xl mx-auto h-[80vh]"> 
    <div class="flex items-center justify-center h-full">
        <!-- Your section content here -->
        <h1 class="text-blue-100 text-GIGA"> {title} </h1>
        <slot/>
    </div>
</section>